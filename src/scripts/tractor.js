// define and export plantSeeds(yearlyPlan) function
// this will iterate over the yearlyPlan string array
// for each element of the array invoke the addPlant() function
// invoke relevant createVegetable() function in seeds directory and return it to the array in fields
